# `report/templates/`

Diretório de **dado**, não de código: templates Jinja2 do relatório de plano
de quitação, com a redação canônica de `Q-03` (`RF-20`, `RF-21`, `RF-22`).

Renderizados a partir de `SnapshotOrdem` e exportados para HTML e PDF pelo
mesmo template (`report/pdf.py`).
