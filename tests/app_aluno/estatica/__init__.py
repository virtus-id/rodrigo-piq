"""Testes estáticos: AST de `app/`, `collection/`, `report/` — as travas da Lei nº 3
(fronteira de import com `engine/`, ausência de conteúdo de questionário no
código, fronteira `Decimal` única e não-modificação de `engine/`/`persistencia/`).
"""
