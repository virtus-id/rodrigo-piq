# `tasks/` — Backlog

Artefatos da fase de **decomposição**. Uma feature por slug:

- `<slug>.tasks.md` — tarefas pequenas, ordenadas e testáveis (`/sdd:tasks`)

Estrutura obrigatória em [`templates/tasks.template.md`](../templates/tasks.template.md).

Consome `plans/<slug>.plan.md` · guia a implementação (`/sdd:implement`).

> Uma tarefa = uma unidade testável. Não coube? Divida. IDs `T-NN` são estáveis
> e nunca renumerados.
